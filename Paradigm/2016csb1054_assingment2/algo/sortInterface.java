// An interface to various sorting algorithm. For sample purpose we have only three sorting algrithm
package algo;
public interface sortInterface{
	void sort(int arr[]);
	void printArray(int arr[]);
}